# nvim-config

## Config

This config is based on open source project https://github.com/nvim-lua/kickstart.nvim

It has a lot of comments originally left by kickstart devs that I did not delete, but can be useful if you are new to nvim configs
